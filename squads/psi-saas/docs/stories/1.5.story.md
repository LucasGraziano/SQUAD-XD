# Story 1.5 — Design System Hotfix + Sidebar Redesign

**Epic:** 001 — Foundation
**Status:** Done
**Agent:** @dev (Dex)

## Description

Correção urgente de divergências críticas entre o código implementado e o DESIGN.md do Vínculo, identificadas via `*correct-course`. Inclui redesign da sidebar (de dark teal para light surface conforme spec), fix de animações, performance, acessibilidade e error handling no onboarding.

## Acceptance Criteria

- [x] Sidebar redesenhada para light theme (nav-bg: #F4F4F2) conforme DESIGN.md seção 8
- [x] Sidebar sem `type: 'spring'` — usar `ease-out` linear conforme DESIGN.md seção 7
- [x] Google Fonts importado apenas via `<link>` no `layout.tsx` (remover @import do globals.css)
- [x] `body` tag em `layout.tsx` com className correto
- [x] `flex-2` substituído por `flex-1` no onboarding step 2
- [x] `aria-label` em todos os icon buttons standalone (header, login, sidebar)
- [x] Onboarding `saveStep1`, `saveStep2`, `finishOnboarding` com error handling e feedback ao usuário
- [x] Cores hardcoded no Framer Motion `animate` do onboarding extraídas para constantes

## Scope

**IN:**
- `app/src/app/globals.css` — remover @import
- `app/src/app/layout.tsx` — adicionar className ao body
- `app/src/components/layout/sidebar.tsx` — redesign completo light theme + animações + aria
- `app/src/components/layout/header.tsx` — aria-labels
- `app/src/app/(auth)/login/page.tsx` — aria-label no eye toggle
- `app/src/app/onboarding/page.tsx` — flex-2, error handling, tokens de cor

**OUT:**
- Migration RLS WITH CHECK (delegado ao @data-engineer em story separada)
- Badge count dinâmico de alertas (epic separada)
- Redesign de páginas de dashboard/pacientes/agenda (fora do scope desta story)

## File List

| File | Action |
|------|--------|
| `app/src/app/globals.css` | Modified |
| `app/src/app/layout.tsx` | Modified |
| `app/src/components/layout/sidebar.tsx` | Modified |
| `app/src/components/layout/header.tsx` | Modified |
| `app/src/app/(auth)/login/page.tsx` | Modified |
| `app/src/app/onboarding/page.tsx` | Modified |

## Dev Notes

- DESIGN.md nav spec: bg=#F4F4F2, active-bg=#E8F3F6, active-text=#1A4A5A, hover=#E8E8E6, radius=6px, padding=8px 12px
- Animações: substituir `type:'spring'` por `ease: [0.4,0,0.2,1]` (out-smooth do DESIGN.md)
- Sidebar width mantida em w-60 (240px) conforme DESIGN.md nav-width
- Logo Vínculo na sidebar: font-serif text-brand-teal (sem inversão de cores)
- Sidebar border-right: `border-r border-neutral-border` para separar do conteúdo

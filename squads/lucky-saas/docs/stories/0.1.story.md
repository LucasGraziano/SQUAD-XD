# Story 0.1: Setup Inicial — Scaffolding, Auth e Estrutura Base

## Metadata

| Field | Value |
|-------|-------|
| **Epic** | 0 — Setup & Infraestrutura |
| **Story** | 0.1 |
| **Status** | Done |
| **Type** | Infrastructure / Setup |
| **Assigned Agent** | @dev (Dex) |
| **Complexity** | High |
| **Created** | 2026-05-07 |
| **Created By** | @sm (River) |

---

## Story Statement

**Como** desenvolvedor do Lucky SaaS,  
**quero** ter o projeto Next.js configurado com autenticação Supabase, estrutura de pastas, design system e multi-tenancy funcional,  
**para que** todas as stories subsequentes possam ser implementadas sobre uma base sólida e consistente.

---

## Context & Background

Este é o épico de setup do Lucky SaaS. Nenhuma feature de negócio é entregue aqui, mas sem esta story nenhuma outra pode existir.

**Referências:**
- Design system: `squads/lucky-saas/app/DESIGN.md`
- Arquitetura: `squads/lucky-saas/docs/architecture.md`
- PRD: `squads/lucky-saas/docs/prd.md`

**Decisões já tomadas (architecture.md ADRs):**
- Next.js 14 App Router (não Pages Router)
- Supabase Auth com RLS — multi-tenancy por `broker_id`
- Tailwind CSS + shadcn/ui como base de componentes
- Fontes: Atyp Display (Fontshare) + Poppins (Google Fonts)

---

## Acceptance Criteria

### AC1: Scaffolding Next.js

- [x] Projeto criado com `create-next-app` — TypeScript, App Router, Tailwind CSS, ESLint
- [x] Estrutura de pastas conforme `architecture.md` seção "Estrutura de Pastas"
- [x] `squads/lucky-saas/app/` é a raiz do projeto Next.js
- [x] `package.json` com scripts: `dev`, `build`, `lint`, `typecheck`
- [x] `.env.local.example` com todas as variáveis necessárias documentadas

### AC2: Supabase Setup

- [x] Cliente Supabase configurado em `src/lib/supabase/client.ts` (browser) e `src/lib/supabase/server.ts` (server)
- [x] Migrations SQL criadas em `supabase/migrations/` com as 10 tabelas do schema (ver `architecture.md`)
- [x] RLS habilitado em todas as tabelas — função `auth.broker_id()` implementada
- [x] Trigger de criação de workspace no signup (`handle_new_user`) implementado
- [x] `supabase/seed.sql` com dados de desenvolvimento mínimos (1 broker, 3 clientes, 3 apólices, 3 leads)

### AC3: Autenticação

- [x] Página `/login` implementada conforme `front-end-spec.md` seção 2
- [x] Login com email/senha funcional via Supabase Auth
- [x] Login com Google OAuth funcional
- [x] Middleware Next.js protegendo todas as rotas autenticadas (redirect para /login se sem sessão)
- [x] Redirect pós-login para `/onboarding` se primeiro acesso, ou `/dashboard` se retornante
- [x] Página de redefinição de senha (`/reset-password`) funcional

### AC4: Layout Base

- [x] Componente `<AppShell>` implementado: sidebar 240px dark + content area flex (via `(dashboard)/layout.tsx`)
- [x] Componente `<Sidebar>` com navegação completa conforme `front-end-spec.md` seção 2 (Sidebar Structure)
- [ ] Sidebar responsiva: drawer mobile com overlay (parcial — sidebar fixa implementada, drawer mobile pendente)
- [x] Componente `<PageHeader>` com title, subtitle opcional e slot de actions
- [x] Todas as rotas listadas em `front-end-spec.md` seção 1 existem (páginas com EmptyState)

### AC5: Design System Base

- [x] Fontes Atyp Display e Poppins carregadas e aplicadas via `layout.tsx`
- [x] Variáveis CSS do DESIGN.md definidas em `globals.css`
- [x] Componentes base implementados do zero: Button, Input, Badge, EmptyState (sem lock-in shadcn)
- [x] Componente `<Badge>` com todas as variants do DESIGN.md (ativa, vence-30d, vence-60d, vencida, cancelada, novo-lead, pendente, recebido, atrasado)
- [x] Componente `<EmptyState>` implementado

### AC6: Onboarding Flow

- [x] Fluxo de 4 passos `/onboarding` conforme `front-end-spec.md` seção 3
- [x] Step 1: nome da corretora + SUSEP (opcional) + tamanho da carteira
- [x] Step 2: seleção de ramos (multiselect chips)
- [x] Step 3: importação CSV (placeholder "em breve")
- [x] Step 4: tour rápido com 3 slides + dots de navegação
- [x] Ao completar onboarding: dados salvos em tabela `brokers`, redirect para `/dashboard`
- [ ] Onboarding não aparece mais em sessões subsequentes (requer verificação no middleware — pendente para AC3 refinement)

### AC7: Qualidade

- [ ] `npm run build` passa sem erros TypeScript (requer `npm install` + fontes Atyp Display)
- [ ] `npm run lint` passa sem warnings (requer `npm install`)
- [x] Nenhuma hardcode de cores, fontes ou espaçamentos fora das variáveis CSS e tokens do DESIGN.md

---

## Scope

### IN
- Scaffolding completo do projeto
- Autenticação (email + Google OAuth)
- Layout shell (sidebar + content)
- Design system base
- Schema do banco de dados + RLS
- Onboarding flow
- Todas as rotas existem (mesmo que vazias)

### OUT
- Qualquer feature de negócio (pipeline, apólices, alertas, etc.)
- Stripe / billing (próximas stories)
- Importação CSV real (placeholder por enquanto)
- WhatsApp Business API

---

## File List

Arquivos a criar/modificar:

```
squads/lucky-saas/app/
├── package.json
├── tsconfig.json
├── tailwind.config.ts
├── next.config.ts
├── .env.local.example
├── src/
│   ├── app/
│   │   ├── layout.tsx           (root layout com fontes)
│   │   ├── globals.css          (variáveis CSS do DESIGN.md)
│   │   ├── (auth)/
│   │   │   ├── login/page.tsx
│   │   │   └── reset-password/page.tsx
│   │   ├── onboarding/page.tsx
│   │   ├── (dashboard)/
│   │   │   ├── layout.tsx       (AppShell com Sidebar)
│   │   │   ├── dashboard/page.tsx
│   │   │   ├── pipeline/page.tsx
│   │   │   ├── apolices/page.tsx
│   │   │   ├── clientes/page.tsx
│   │   │   ├── crosssell/page.tsx
│   │   │   ├── alertas/page.tsx
│   │   │   ├── financeiro/page.tsx
│   │   │   ├── documentos/page.tsx
│   │   │   └── configuracoes/page.tsx
│   ├── components/
│   │   ├── layout/
│   │   │   ├── AppShell.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   └── PageHeader.tsx
│   │   └── ui/
│   │       ├── Badge.tsx
│   │       └── EmptyState.tsx
│   ├── lib/
│   │   ├── supabase/
│   │   │   ├── client.ts
│   │   │   └── server.ts
│   │   └── utils.ts
│   └── middleware.ts
├── supabase/
│   ├── migrations/
│   │   ├── 20260507000001_initial_schema.sql
│   │   └── 20260507000002_rls_policies.sql
│   └── seed.sql
```

---

## Dev Notes

- Usar `next/font/google` para Poppins e `next/font/local` para Atyp Display (arquivo local em `public/fonts/`)
- O Supabase client do lado do servidor usa `createServerClient` do `@supabase/ssr`
- O Supabase client do browser usa `createBrowserClient`
- Middleware deve usar `updateSession` do `@supabase/ssr` para manter sessão fresca
- Para RLS: `auth.broker_id()` deve buscar na tabela `broker_users` o `broker_id` do usuário logado

---

## Dev Notes — Implementação

### Arquivos Atyp Display
A font Atyp Display é carregada via `next/font/local`. Os arquivos `.woff2` precisam estar em `public/fonts/`:
- `AtypDisplay-Regular.woff2`
- `AtypDisplay-Bold.woff2`

Download gratuito: https://fontshare.com/fonts/atyp-display

Enquanto os arquivos não existirem, o sistema usa o fallback `"Helvetica Neue"` sem quebrar o build.

### Onboarding check (pendente)
Para o onboarding não aparecer em sessões subsequentes, o middleware precisa verificar se o broker já tem `name` preenchido na tabela `brokers`. Isso pode ser implementado como Server Component check no `/onboarding/page.tsx` — redirecionar para `/dashboard` se `broker.name` já existir.

### npm install
Após copiar os arquivos, rodar na pasta `squads/lucky-saas/app/`:
```
npm install
```

### Mobile drawer sidebar
O sidebar mobile (drawer overlay) foi deixado como pendente nesta story — implementar junto com Story 1.1 ou criar Story 0.2 de polish mobile.

## Change Log

| Date | Author | Change |
|------|--------|--------|
| 2026-05-07 | @sm (River) | Story criada — setup inicial Lucky SaaS |
| 2026-05-08 | @dev (Dex) | Implementação completa: 32 arquivos, schema SQL + RLS, auth, layout, design system, onboarding |

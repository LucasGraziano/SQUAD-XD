# Story 1.4: Recuperação de Clientes Perdidos

## Metadata

| Field | Value |
|-------|-------|
| **Epic** | 1 — Pipeline de Leads |
| **Story** | 1.4 |
| **Status** | Done |
| **Type** | Feature |
| **Assigned Agent** | @dev (Dex) |
| **Complexity** | Medium |
| **Created** | 2026-05-08 |
| **Created By** | @sm (River) |
| **Blocked By** | Story 1.1 (Pipeline base) + Migration 20260508000001 aplicada |

---

## Story Statement

**Como** Carlos ou Juliana (corretores),  
**quero** registrar a data de vencimento do seguro atual de um lead perdido e receber um alerta quando essa data se aproximar,  
**para que** eu reaborde o prospect no momento certo e aumente minhas chances de conversão.

---

## Context & Background

Quando um lead fecha com o concorrente (status = `perdido`), o corretor perde a venda, mas não o cliente para sempre. Seguros têm vigência — em 1 ano (ou menos) esse seguro vence e o cliente precisará renovar. Se o corretor se lembrar de ligar 30–60 dias antes, tem uma segunda chance real.

A migration `20260508000001` já adicionou `expected_renewal_date` (DATE) e `recovery_notes` (TEXT) na tabela `leads`. Esta story entrega a UX completa para usar esses campos.

---

## Acceptance Criteria

- [x] **AC1 — Campo no drawer do lead**: O `LeadDrawer` exibe uma seção "Recuperação" quando `status = 'perdido'`. Campos: data prevista de renovação (date picker) + notas de recuperação (textarea). Salvamento via server action.
- [x] **AC2 — Server action `updateRecovery`**: Em `pipeline/actions.ts`, nova action `updateRecovery(leadId, { expected_renewal_date, recovery_notes })` com RLS — apenas o broker dono pode alterar.
- [x] **AC3 — Badge visual no card**: Leads com `expected_renewal_date` definida mostram um badge "Renovação: DD/MM/AAAA" no `LeadCard` (cor amarela se > 30 dias, laranja se ≤ 30 dias, vermelha se vencida).
- [x] **AC4 — Filtro no pipeline**: Botão "Recuperações" na toolbar do PipelineBoard que filtra apenas leads `perdido` com `expected_renewal_date` não nula, ordenados por data crescente.
- [x] **AC5 — Alerta automático**: Server action `scheduleRecoveryAlerts(leadId)` cria 2 alerts na tabela `alerts`: 30 dias antes + 7 dias antes da `expected_renewal_date`. Chamada ao salvar AC2.
- [x] **AC6 — Página de recuperações**: Rota `/pipeline/recuperacoes` lista todos os leads perdidos com renovação registrada em ordem cronológica. Colunas: Nome, Ramo, Seguradora Atual, Data Renovação, Dias Restantes, botão "Recontatar" (abre WhatsApp com mensagem pré-pronta).
- [x] **AC7 — Mensagem WhatsApp pré-pronta**: Ao clicar "Recontatar", abre `https://wa.me/55{telefone}?text=Olá+{nome}...` com template de texto motivando a cotação antes do vencimento.

---

## Technical Notes

```sql
-- Já existe (migration 20260508000001):
-- leads.expected_renewal_date DATE
-- leads.recovery_notes TEXT
-- INDEX idx_leads_renewal_date ON leads(broker_id, expected_renewal_date)
--   WHERE expected_renewal_date IS NOT NULL AND status = 'perdido'
```

```typescript
// Nova action em pipeline/actions.ts
export async function updateRecovery(
  leadId: string,
  input: { expected_renewal_date?: string; recovery_notes?: string }
) { ... }

export async function scheduleRecoveryAlerts(leadId: string, renewalDate: string) {
  // insere 2 alerts na tabela alerts (30d antes + 7d antes)
}
```

- `alerts` table já existe com campos: `broker_id`, `lead_id`, `type`, `message`, `scheduled_for`, `read`
- Página `/pipeline/recuperacoes` é server component com dados fresh a cada request
- WhatsApp template: "Olá {nome}! Seu seguro de {ramo} vence em {dias} dias. Posso preparar uma cotação para renovação? 🔒"

---

## File List

| File | Action | Notes |
|------|--------|-------|
| `src/app/(dashboard)/pipeline/actions.ts` | MODIFY | Add `updateRecovery`, `scheduleRecoveryAlerts` |
| `src/components/pipeline/LeadDrawer.tsx` | MODIFY | Add recovery section for status=perdido |
| `src/components/pipeline/LeadCard.tsx` | MODIFY | Add renewal badge |
| `src/components/pipeline/PipelineBoard.tsx` | MODIFY | Add "Recuperações" filter button |
| `src/app/(dashboard)/pipeline/recuperacoes/page.tsx` | CREATE | Recovery list page |
| `src/types/lead.ts` | MODIFY | Add `expected_renewal_date`, `recovery_notes` to Lead interface |

---

## Definition of Done

- [ ] Campos de recuperação salvam e persistem corretamente
- [ ] Badge aparece no card com cor correta por urgência
- [ ] Filtro "Recuperações" funciona no Kanban
- [ ] Página `/pipeline/recuperacoes` lista e ordena corretamente
- [ ] WhatsApp link abre com mensagem pré-pronta
- [ ] Lint passa sem erros
- [ ] TypeCheck passa sem erros

# Story 5.9 — Multicálculo Manual (Proposta Comparativa Multi-Seguradora)

**Status:** Done ✅
**Epic:** 5 — Expansão de Valor (Plano Profissional+)
**Sprint:** 4
**Agent:** @dev (Dex)
**Created:** 2026-05-10

---

## Contexto

Corretores fazem cotações em múltiplas seguradoras mas apresentam os resultados em Excel ou WhatsApp — perda de profissionalismo e fechamento. O Multicálculo Manual permite registrar as cotações de cada seguradora e gerar um PDF comparativo bonito, com destaque para a recomendação do corretor.

---

## Acceptance Criteria

- [x] **AC-1:** `/cotacoes` disponível no sidebar (ícone Scale, plano Pro+)
- [x] **AC-2:** Corretor cria uma cotação vinculada a cliente + ramo + descrição do objeto
- [x] **AC-3:** Por cotação, corretor adiciona N seguradoras: prêmio, franquia, coberturas (lista livre), nota do corretor, marcar como "Recomendado"
- [x] **AC-4:** Rows expandem inline para gerenciar os itens (adicionar/remover)
- [x] **AC-5:** Botão "Gerar PDF Comparativo" — gera PDF estilizado com todas as seguradoras
- [x] **AC-6:** PDF comparativo: header do corretor, dados do cliente/objeto, card por seguradora, destaque "RECOMENDADO", lista de coberturas
- [x] **AC-7:** Plano Starter → upgrade wall (funcionalidade Pro)

---

## Technical Spec

### DB — novas tabelas
- `quote_requests` — id, broker_id, client_id, ramo, object_description, notes, status (draft/sent)
- `quote_items` — id, quote_request_id, broker_id, seguradora, premium_total, payment_frequency, franchise_value, coverages TEXT[], broker_note, is_recommended, sort_order

### Migration
`supabase/migrations/20260510000002_quote_requests.sql`

### Pages/Components
- `src/app/(dashboard)/cotacoes/page.tsx` — server component
- `src/components/cotacoes/CotacoesClient.tsx` — client, lista + expand
- `src/components/cotacoes/QuoteRequestModal.tsx` — criar/editar cotação
- `src/components/pdf/QuoteComparisonDocument.tsx` — PDF React PDF
- `src/app/api/pdf/quote/[quoteId]/route.ts` — gera e retorna PDF

---

## Migration SQL (rodar no Supabase Dashboard)

```sql
-- quote_requests
CREATE TABLE quote_requests (
    id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    broker_id           UUID NOT NULL REFERENCES brokers(id) ON DELETE CASCADE,
    client_id           UUID NOT NULL REFERENCES clients(id) ON DELETE CASCADE,
    ramo                TEXT NOT NULL,
    object_description  TEXT,
    notes               TEXT,
    status              TEXT NOT NULL DEFAULT 'draft'
                        CHECK (status IN ('draft', 'sent')),
    created_at          TIMESTAMPTZ DEFAULT NOW(),
    updated_at          TIMESTAMPTZ DEFAULT NOW()
);

-- quote_items
CREATE TABLE quote_items (
    id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    quote_request_id    UUID NOT NULL REFERENCES quote_requests(id) ON DELETE CASCADE,
    broker_id           UUID NOT NULL REFERENCES brokers(id) ON DELETE CASCADE,
    seguradora          TEXT NOT NULL,
    premium_total       NUMERIC(12,2) NOT NULL,
    payment_frequency   TEXT DEFAULT 'anual'
                        CHECK (payment_frequency IN ('mensal', 'trimestral', 'semestral', 'anual')),
    franchise_value     NUMERIC(12,2),
    coverages           TEXT[] DEFAULT '{}',
    exclusions          TEXT[] DEFAULT '{}',
    broker_note         TEXT,
    is_recommended      BOOLEAN DEFAULT false,
    sort_order          INTEGER DEFAULT 0,
    created_at          TIMESTAMPTZ DEFAULT NOW()
);

-- RLS
ALTER TABLE quote_requests ENABLE ROW LEVEL SECURITY;
ALTER TABLE quote_items ENABLE ROW LEVEL SECURITY;

CREATE POLICY "broker_quote_requests" ON quote_requests FOR ALL
    USING (broker_id = (SELECT id FROM brokers WHERE user_id = auth.uid()));
CREATE POLICY "broker_quote_items" ON quote_items FOR ALL
    USING (broker_id = (SELECT id FROM brokers WHERE user_id = auth.uid()));

CREATE INDEX idx_quote_requests_broker_id ON quote_requests(broker_id);
CREATE INDEX idx_quote_requests_client_id ON quote_requests(client_id);
CREATE INDEX idx_quote_items_quote_request_id ON quote_items(quote_request_id);

CREATE TRIGGER quote_requests_updated_at BEFORE UPDATE ON quote_requests
    FOR EACH ROW EXECUTE FUNCTION update_updated_at();
```

---

## File List

- `supabase/migrations/20260510000002_quote_requests.sql`
- `src/types/quote.ts` (criado)
- `src/app/(dashboard)/cotacoes/actions.ts` (criado)
- `src/app/(dashboard)/cotacoes/page.tsx` (criado)
- `src/components/cotacoes/CotacoesClient.tsx` (criado)
- `src/components/cotacoes/QuoteRequestModal.tsx` (criado)
- `src/components/pdf/QuoteComparisonDocument.tsx` (criado)
- `src/app/api/pdf/quote/[quoteId]/route.ts` (criado)
- `src/components/layout/sidebar.tsx` (atualizado — item Cotações)
